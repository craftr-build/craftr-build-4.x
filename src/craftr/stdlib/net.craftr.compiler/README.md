# Namespace: `net.craftr.compiler`

This namespace implements the compiler detection suites for MSVC, MinGW
and LLVM. These are mostly required for Windows to reduce the manual
environment configuration hazzle.

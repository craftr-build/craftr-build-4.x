### `craftr/examples/java`

This example demonstrates how to use the `craftr/lang/java` module to build
a simple Java application and bundling them into a single executable JAR.

Targets:

- `main:java.javac`
- `main:java.bundle`
- `main:java.runBundle`

# examples.java

from craftr.lang import java
javac = java.JavaCompiler()

classes = javac.compile(
  srcs = [local('Main.java')],
  src_dir = project_dir
)

jar = javac.make_jar(
  inputs = classes,
  output = 'java-example'
)

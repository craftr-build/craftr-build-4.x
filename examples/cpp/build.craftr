
apply 'cxx'

c_library('array') {
  public_include_paths = [file('src/lib')]
  sources = glob('src/lib/*.c')
}

cpp_application('main') {
  sources = glob('src/*.cpp')
  dependencies.append tasks.array
}

run {
  dependencies.append tasks.main
}

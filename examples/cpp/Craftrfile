# cpp

cxc = load_module('lang.cxx.clang').cxc

obj = cxc.compile(
  language = 'c++',
  sources = [local('main.cpp')]
)

main = cxc.link(
  output_type = 'bin',
  inputs = obj,
  output = 'main'
)

run = gentarget([[main]])

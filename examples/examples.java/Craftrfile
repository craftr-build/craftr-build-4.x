# examples.java

javac = load_module('lang.java').javac

classes = javac.compile(
  srcs = [local('Main.java')],
  src_dir = project_dir
)

jar = javac.make_jar(
  inputs = classes,
  entry_point = 'Main',
  output = 'java-example'
)

run = gentarget([['java', '-jar', jar]], explicit=True)

project "tools.msvc"

options:
  str installDir = ''
  str version = ''
  str arch = 'x86' if OS.arch == 'x86' else 'x86_amd64'
  str platformType = ''
  str sdkVersion = ''

eval:
  module.nodepy_module.exports = require('./msvc')
